#!/bin/sh

# *--------------------------------------------------------------*

RELATIVE=`dirname "$0"`/..
HOME="`cd \"$RELATIVE\" 2>/dev/null && pwd || echo \"$RELATIVE\"`"

# *--------------------------------------------------------------*

$HOME/.build/debug/MKTKoansRunner $0 $@

# *--------------------------------------------------------------*
